export enum Collection {
  USERS = "users",
  QUIZZES = "quizzes",
  QUESTIONS = "questions",
  QUIZRESULTS = "QuizResults",
}

export enum HttpMethod {
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  GET = "GET",
}
